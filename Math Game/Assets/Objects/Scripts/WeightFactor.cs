using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeightFactor : MonoBehaviour
{
    // Start is called before the first frame update
    public float weight;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public float GetWeightValue()
    {
        return weight; 
    }
}
